{
  "version": 2,
  "builds": [
    {
      "src": "./server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**/*.ts", "server.ts", "api/**/*.ts"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
